server:
  port: 8081

spring:
  application:
    name: SERVICE-CLIENT

  datasource:
    url: jdbc:mysql://localhost:3306/emploi
    username: root
    password:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
